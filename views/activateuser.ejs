<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  <title>Document</title>
</head>
<body>
  <div>
    <div class="columns is-mobile is-centered">
      <div class="column is-half box">
        <header class="is-size-3 has-text-weight-bold block">Activate User</header>
        <div class="block">
          Are you sure you want to activate user <%= user.firstName %> <%= user.lastName %>
        </div>
        <div class="buttons block">
          <form action="/api/activate-user/<%= user.id %>" method="POST">
            <button class="button is-primary">
              Activate
            </button>
          </form>
          <a href="/admin" class="button is-light">Cancel</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>